# FitrQuest

### What I learnt
- JWT Tokens was a first for me also and I was intrigued on how it is used in applications. As I wanted to create a page for only signed up users this was perfect as JWT verifies identities and protects routes.

- Using AuthContext I learnt that it tracks authentication state across the app. Letting components check whether a user is logged in without passing props, making it an easier experience for me development wise.

- Using MongoDB, I learned how to use schemas to define structured fields like name, email, and password for login/signup.

<br />

### Improvements for the future
- Regarding the schema I will look to apply rules such as required and unique to enforce data integrity and simplify management.
  
- Provide more questions to get a more refined/accurate end result for users
  
- Allow user to create a new or recover their password

<br />

## Deployment
### PROJECT INSTALLATION
 * In this project directory you will need `node.js` and `npm` installed globally on your system.
 
1.	To **install** project dependencies by running `npm install`
2.	To run test `npm test`
3.	To start the server `npm run dev`
4.	Once server is started view the app in ` http://localhost:5173`

<br />
